const btn=document.querySelector(".navbutton"),menu=document.querySelector(".menu");if(btn&&menu){btn.addEventListener("click",()=>{const o=menu.classList.toggle("open");btn.classList.toggle("open",o);btn.setAttribute("aria-expanded",String(o))});menu.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{menu.classList.remove("open");btn.classList.remove("open");btn.setAttribute("aria-expanded","false")}))}const obs=new IntersectionObserver(es=>{es.forEach(e=>{if(e.isIntersecting)e.target.classList.add("visible")})},{threshold:.14});document.querySelectorAll(".reveal").forEach(e=>obs.observe(e));const c=document.getElementById("ambient"),x=c?.getContext("2d");let pts=[],raf;function resize(){if(!c||!x)return;c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);const n=Math.min(70,Math.max(30,Math.floor(innerWidth/24)));pts=Array.from({length:n},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.22,vy:(Math.random()-.5)*.22}))}function draw(){if(!c||!x)return;x.clearRect(0,0,innerWidth,innerHeight);for(const p of pts){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>innerWidth)p.vx*=-1;if(p.y<0||p.y>innerHeight)p.vy*=-1}for(let i=0;i<pts.length;i++){for(let j=i+1;j<pts.length;j++){const a=pts[i],b=pts[j],dx=a.x-b.x,dy=a.y-b.y,d=Math.hypot(dx,dy);if(d<150){x.strokeStyle=`rgba(37,99,235,${.09*(1-d/150)})`;x.lineWidth=1;x.beginPath();x.moveTo(a.x,a.y);x.lineTo(b.x,b.y);x.stroke()}}}for(const p of pts){x.fillStyle="rgba(20,184,200,.20)";x.beginPath();x.arc(p.x,p.y,2,0,Math.PI*2);x.fill()}raf=requestAnimationFrame(draw)}if(!matchMedia("(prefers-reduced-motion: reduce)").matches){resize();draw();addEventListener("resize",()=>{cancelAnimationFrame(raf);resize();draw()})}const form=document.getElementById("contactForm"),status=document.getElementById("formStatus");if(form&&status){form.addEventListener("submit",async e=>{e.preventDefault();const endpoint=form.getAttribute("action")||"";if(!endpoint||endpoint==="COLOCAR_ENDPOINT_AQUI"){status.textContent="Falta configurar o endpoint do formulário.";status.style.color="#8a4b00";return}status.textContent="A enviar...";try{const r=await fetch(endpoint,{method:"POST",body:new FormData(form),headers:{Accept:"application/json"}});if(r.ok){form.reset();status.textContent="Mensagem enviada.";status.style.color="#0c7a43"}else{status.textContent="Erro no envio. Verifique o endpoint.";status.style.color="#a12a2a"}}catch(err){status.textContent="Falha de rede. Tente novamente.";status.style.color="#a12a2a"}})}